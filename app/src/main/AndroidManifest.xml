<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.connectify.connectify">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Connectify"
        tools:targetApi="31">

        <!-- Choose Role -->
        <activity android:name=".ChooseRoleActivity" />

        <!-- 🔥 Splash as Launcher -->
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.Connectify">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- MainActivity (not launcher) -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Connectify" />

        <!-- Login Activity -->
        <activity
            android:name=".LoginActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Connectify" />

        <!-- Register Activity -->
        <activity android:name=".RegisterActivity" android:exported="true" />

        <!-- Other Activities -->
        <activity android:name=".HomeActivity" />
        <activity android:name=".JobsActivity" />
        <activity android:name=".SeekerProfileActivity" />
        <activity android:name=".JobSeekerDashboard" />
        <activity android:name=".ChatActivity" />
        <activity android:name=".EmployerProfileActivity" />
        <activity android:name=".CompanyProfileActivity" />
        <activity android:name=".SeekerMainProfileActivity" />
        <activity android:name=".EditSeekerProfileActivity" />
        <activity android:name=".SwipedMatchesActivity" />

        <!-- Employer Dashboard with singleTop launch mode -->
        <activity
            android:name=".EmployerDashboardActivity"
            android:launchMode="singleTop"
            android:windowSoftInputMode="adjustPan" />

        <!-- Lottie Loader for transitions -->
        <activity
            android:name=".LottieLoaderActivity"
            android:exported="false" />

    </application>
</manifest>
